# Backtest Cache

Place local candle and generated backtest JSON/CSV files here when running scripts in `tools/backtests/`.

These files are ignored by Git because they are large and environment-specific.
