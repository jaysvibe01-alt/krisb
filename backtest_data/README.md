# Backtest Data

This directory is for local backtest caches and generated result files copied from the research workspace.

Large candle/result files are intentionally ignored by Git. Keep reproducible scripts under `tools/backtests/` and place local JSON/CSV data here only when running checks on this machine.
